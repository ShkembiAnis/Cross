import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Anis_Aufgabe1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
